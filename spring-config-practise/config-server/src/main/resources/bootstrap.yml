server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/summer9989/spring-config-repository
encrypt:
  key-store:
    location: classpath:/spring-config.jks
    password: storepass
    alias: testkey
    secret: keypass
