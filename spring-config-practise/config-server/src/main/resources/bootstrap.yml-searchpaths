server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/summer9989/spring-config-repository
          #search-paths:
          #  - app2
          search-paths: '{application}'
