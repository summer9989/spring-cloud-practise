server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/summer9989/spring-config-repository
  security:
    user:
      name: admin
      password: admin
security:
  basic:
    enabled: true
